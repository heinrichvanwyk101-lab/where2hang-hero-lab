# where2hang-hero-lab
where2hang-hero-lab/   index.html        ← the demo page (GitHub Pages serves it live)   city-hero.js      ← THE LEAF: the WebGL hero, framework-agnostic   city-state.js     ← THE DATA SEAM: mock now → Supabase later   README.md
